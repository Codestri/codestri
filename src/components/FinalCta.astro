---
interface Props {
  title: string;
  description?: string;
  actionLabel?: string;
  actionHref?: string;
}

const {
  title,
  description = '',
  actionLabel = 'Get Started',
  actionHref = '/contact',
} = Astro.props as Props;
---

<section class="mt-10 rounded-2xl border border-violet-300/30 bg-gradient-to-b from-violet-500/10 to-black/20 p-6 md:p-8">
  <h2 class="text-2xl font-semibold text-white">{title}</h2>
  {description && <p class="mt-3 max-w-2xl text-slate-300">{description}</p>}
  <a
    href={actionHref}
    class="mt-6 inline-flex rounded-lg bg-violet-400 px-5 py-3 text-sm font-bold text-black transition hover:bg-violet-300"
  >
    {actionLabel}
  </a>
</section>
