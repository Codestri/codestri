---
interface Props {
  items: string[];
}

const { items = [] } = Astro.props as Props;
---

<ul class="space-y-2">
  {items.map((item) => (
    <li class="flex items-start gap-3 text-sm text-slate-200">
      <span class="mt-0.5 inline-flex h-5 w-5 items-center justify-center rounded-full bg-violet-400/20 text-violet-300">✓</span>
      <span>{item}</span>
    </li>
  ))}
</ul>
